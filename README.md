# Home test for CastLabs

Run the following command to install the dependencies:
```bash
npm install
```

Run the following command to start the server:
```bash
npm run dev
```

The server will be running on `http://localhost:5173`.


